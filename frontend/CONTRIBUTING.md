# Руководство для разработчиков Pin

## Быстрый старт для новых разработчиков

### 1. Подготовка окружения

```bash
# Клонирование репозитория
git clone <repository-url>
cd Pin/frontend

# Установка зависимостей
npm install

# Создание файла переменных окружения (опционально)
cp .env.example .env.local
```

### 2. Запуск приложения

```bash
# Запуск оба сервера одновременно (рекомендуется)
npm run dev:all

# Или запустите в отдельных терминалах:
# Терминал 1:
npm run dev

# Терминал 2:
npm run dev:server
```

### 3. Открытие в браузере

Откройте [http://localhost:3000](http://localhost:3000)

## Основные команды

```bash
# Развитие
npm run dev              # Запустить Next.js на http://localhost:3000
npm run dev:server       # Запустить json-server на http://localhost:3001
npm run dev:all          # Запустить оба одновременно

# Продакшн
npm run build            # Собрать приложение
npm start                # Запустить собранное приложение
npm run lint             # Проверить код на ошибки
```

## Структура файлов и назначение

### `/components` - React компоненты

- **Map.tsx** - Основной компонент карты (Leaflet)
  - Отображает маркеры пинов
  - Обрабатывает клики по карте и маркерам
  - Поддерживает drag-and-drop для своих пинов
- **MapView.tsx** - Контейнер для карты

  - Управляет состоянием пинов
  - Обрабатывает API запросы
  - Контролирует отображение модальных окон

- **PinCard.tsx** - Карточка пина (отображается снизу на мобилке/браузере)

  - Показывает детали пина
  - Кнопка "Связаться" для звонка
  - Кнопка "Удалить Pin" для своих пинов

- **CreatePinForm.tsx** - Форма создания нового пина

  - Валидация полей
  - Отправка на API

- **ContactModal.tsx** - Модальное окно для ввода контактов

- **MapInstruction.tsx** - Инструкция на карте (только в браузере)

  - Отображает "👆 Коснитесь точки..." только в браузере
  - На мобилке скрыта

- **TelegramProvider.tsx** - Провайдер для Telegram контекста

### `/hooks` - Custom React hooks

- **useTelegram.ts** - Управление Telegram Web App API
  - Инициализация WebApp
  - Функции expand(), close(), showAlert() и т.д.
  - Блокировка ориентации экрана
  - Отключение закрытия по кнопке "назад"

### `/lib` - Утилиты и помощники

- **api.ts** - HTTP клиент

  - GET, POST, PATCH, DELETE методы
  - Автоматическое управление ошибками
  - Базовый URL из переменных окружения

- **telegram.ts** - Telegram утилиты

### `/types` - TypeScript интерфейсы

- **index.ts** - Основные типы
  - `Pin` - интерфейс для пина
  - `User` - интерфейс для пользователя
  - `CreatePinData` - данные для создания пина

## Рабочий процесс разработки

### Добавление новой функции

1. **Создайте компонент** (если нужно)

   ```tsx
   // components/NewComponent.tsx
   import { useState } from 'react';

   export default function NewComponent() {
     return <div>Hello</div>;
   }
   ```

2. **Добавьте типы** (если нужны)

   ```tsx
   // types/index.ts
   export interface NewType {
     id: string;
     name: string;
   }
   ```

3. **Используйте в других компонентах**

   ```tsx
   import NewComponent from '@/components/NewComponent';

   export default function Page() {
     return <NewComponent />;
   }
   ```

4. **Тестируйте в браузере**
   - Откройте DevTools (F12)
   - Проверьте консоль на ошибки
   - Проверьте Network вкладку для API запросов

### Работа с API

```tsx
// Получение данных
const loadPins = async () => {
  const data = await api.get<Pin[]>('/pins');
  setPins(data);
};

// Создание данных
const createPin = async (pinData: CreatePinData) => {
  const newPin = await api.post<Pin>('/pins', pinData);
  setPins([...pins, newPin]);
};

// Обновление данных
const updatePin = async (pinId: string, updates: Partial<Pin>) => {
  const updated = await api.patch<Pin>(`/pins/${pinId}`, updates);
  setPins(pins.map((p) => (p.id === pinId ? updated : p)));
};

// Удаление данных
const deletePin = async (pinId: string) => {
  await api.delete(`/pins/${pinId}`);
  setPins(pins.filter((p) => p.id !== pinId));
};
```

## Стили и темы

### CSS переменные (в `globals.css`)

```css
:root {
  /* Светлая тема */
  --tg-theme-bg-color: #ffffff;
  --tg-theme-text-color: #000000;

  /* Pin цвета */
  --pin-primary: #1a5f3f; /* Зеленый для чужих пинов */
  --pin-primary-light: #2d8a5f;
  --pin-accent: #4caf50;
}
```

### Tailwind классы

```tsx
// Основные классы
<div className="bg-white text-black p-4 rounded-lg">
  <h1 className="text-xl font-bold">Заголовок</h1>
  <p className="text-gray-700">Описание</p>
</div>

// Pin цвета (расширенные в tailwind.config.ts)
<button className="bg-pin-primary text-white">Кнопка</button>

// Telegram цвета
<div className="bg-telegram-bg text-telegram-text">
  Контент
</div>
```

## Отладка

### Консоль браузера

```javascript
// Посмотреть состояние пинов
console.log(pins);

// Проверить API запрос
fetch('/pins')
  .then((r) => r.json())
  .then(console.log);

// Проверить Telegram объект
console.log(window.Telegram?.WebApp);
```

### Network вкладка DevTools

1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Выполните действие (например, создание пина)
4. Проверьте запрос:
   - Метод: POST, GET, PATCH, DELETE
   - URL: правильный адрес API
   - Статус: 200, 201, 204 - успех, 400+ - ошибка

### Логирование

```tsx
// Добавьте логи для отладки
console.log('Creating pin:', data);
try {
  const result = await api.post('/pins', data);
  console.log('Pin created:', result);
} catch (error) {
  console.error('Error creating pin:', error);
}
```

## Тестирование в Telegram

### Локальное тестирование с ngrok

```bash
# 1. Установите ngrok
# https://ngrok.com/download

# 2. Запустите ngrok
ngrok http 3000

# 3. Используйте полученный URL в BotFather
# Например: https://abc123.ngrok.io
```

### На реальном устройстве

1. Откройте бота в Telegram
2. Нажмите на Web App
3. Приложение загружается в WebView
4. Для отладки используйте DevTools в Telegram Desktop

## Оптимизация производительности

### Code Splitting

Next.js автоматически разбивает код, но для больших компонентов используйте dynamic import:

```tsx
import dynamic from 'next/dynamic';

const Map = dynamic(() => import('@/components/Map'), {
  ssr: false,
  loading: () => <div>Загрузка карты...</div>,
});
```

### Мемоизация

Используйте `useMemo` и `useCallback` для предотвращения ненужных перерендеров:

```tsx
const memoizedValue = useMemo(() => {
  return expensiveCalculation(a, b);
}, [a, b]);

const memoizedCallback = useCallback(() => {
  doSomething(a, b);
}, [a, b]);
```

## Безопасность

### Переменные окружения

Используйте переменные окружения для конфиденциальных данных:

```env
NEXT_PUBLIC_API_URL=http://localhost:3001  # Видна в клиенте
```

Не добавляйте `.env.local` в git!

### Валидация данных

Всегда валидируйте входные данные с сервера:

```tsx
if (!pin.id || !pin.title) {
  console.error('Invalid pin data');
  return;
}
```

## Лучшие практики

### 1. Именование компонентов

```tsx
// ✅ Хорошо - PascalCase
export default function PinCard() {}

// ❌ Плохо - camelCase
export default function pinCard() {}
```

### 2. Импорты

```tsx
// ✅ Хорошо - используйте @ alias
import { Pin } from '@/types';
import MapView from '@/components/MapView';

// ❌ Плохо - относительные пути
import { Pin } from '../../../types';
import MapView from '../../components/MapView';
```

### 3. Состояние

```tsx
// ✅ Хорошо - разбивайте состояние на логические части
const [selectedPin, setSelectedPin] = useState<Pin | null>(null);
const [isLoading, setIsLoading] = useState(false);
const [error, setError] = useState<string | null>(null);

// ❌ Плохо - большой объект состояния
const [state, setState] = useState({
  selectedPin: null,
  isLoading: false,
  error: null,
});
```

### 4. Обработка ошибок

```tsx
// ✅ Хорошо
try {
  const data = await api.post('/pins', pinData);
  setPins([...pins, data]);
} catch (error) {
  console.error('Failed to create pin:', error);
  alert('Ошибка при создании Pin');
}

// ❌ Плохо - игнорирование ошибок
api.post('/pins', pinData).then((data) => {
  setPins([...pins, data]);
});
```

### 5. Комментарии

```tsx
// ✅ Хорошо - описание сложной логики
// Блокируем закрытие приложения в Telegram при навигации
if (typeof WebApp !== 'undefined') {
  WebApp.onEvent('backButtonClicked', () => {
    // Ничего не делаем - отключаем закрытие
  });
}

// ❌ Плохо - очевидные комментарии
// Устанавливаем loading в true
setLoading(true);
```

## Решение проблем

### Проблема: "Cannot find module '@/components/...'"

**Решение:** Проверьте, что в `tsconfig.json` правильно настроен alias:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

### Проблема: "CORS error" при запросе к API

**Решение:** Убедитесь, что json-server запущен и доступен:

```bash
# Проверьте, что сервер работает
curl http://localhost:3001/pins

# Если не работает, перезапустите
npm run dev:server
```

### Проблема: Карта не отображается

**Решение:** Проверьте консоль браузера на ошибки. Убедитесь, что:

1. Компонент Map загружается dynamically (ssr: false)
2. Контейнер карты имеет размер (width, height)
3. Leaflet CSS загружается

### Проблема: Пины не обновляются при редактировании

**Решение:** Убедитесь, что вы правильно обновляете состояние:

```tsx
// ✅ Хорошо - создаете новый массив
setPins(pins.map((p) => (p.id === updatedPin.id ? updatedPin : p)));

// ❌ Плохо - мутируете существующий массив
pins[0] = updatedPin;
setPins(pins);
```

## Контакты и поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию в README.md
2. Посмотрите MONGODB_SCHEMA.md для информации о БД
3. Откройте issue в репозитории
4. Обратитесь к team lead

## Полезные ссылки

- [Next.js документация](https://nextjs.org/docs)
- [React Leaflet](https://react-leaflet.js.org/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [TypeScript](https://www.typescriptlang.org/docs/)
- [Telegram Web App API](https://core.telegram.org/bots/webapps)
- [MongoDB документация](https://docs.mongodb.com/)
